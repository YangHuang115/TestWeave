# 仅供本地开发。复制为 .env 后请自行替换；不得用于生产环境。
POSTGRES_DB=testweave
POSTGRES_USER=testweave
POSTGRES_PASSWORD=local-development-only-change-me
# 注意：POSTGRES_PORT 与下方 TESTWEAVE_DATABASE_URL 中内嵌的端口是两处重复，两处必须保持一致。
POSTGRES_PORT=5432

# 开发服务端口（唯一权威默认值在 Makefile 的 SERVER_PORT ?= / WEB_PORT ?=，vite.config.ts 以同名环境变量跟随）。
# 需要自定义时取消注释并以环境变量方式生效（例如 `SERVER_PORT=8100 make server`）。
# SERVER_PORT=8000
# 自定义 WEB_PORT 时必须同步修改下方 TESTWEAVE_CORS_ORIGINS 中的端口，两者必须指向同一前端端口，否则浏览器跨域请求会被拒绝。
# WEB_PORT=5173

TESTWEAVE_ENVIRONMENT=development
TESTWEAVE_LOG_LEVEL=INFO
# 注意：本行 URL 内嵌的端口必须与上方 POSTGRES_PORT 保持一致。
TESTWEAVE_DATABASE_URL=postgresql+psycopg://testweave:local-development-only-change-me@127.0.0.1:5432/testweave
TESTWEAVE_DATABASE_CONNECT_TIMEOUT_SECONDS=5
TESTWEAVE_DATABASE_POOL_TIMEOUT_SECONDS=5
TESTWEAVE_DATABASE_STATEMENT_TIMEOUT_MS=5000
TESTWEAVE_MIGRATION_STATEMENT_TIMEOUT_MS=900000
TESTWEAVE_MIGRATION_LOCK_TIMEOUT_MS=10000
TESTWEAVE_CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
# 仅供本地开发的占位加密主密钥。生产必须通过秘密管理注入至少 32 字符的高熵随机值，严禁复用此值。
# 启用 Android MCP 时也必须替换为高熵随机值，用于派生不透明设备引用。
TESTWEAVE_SECRET_KEY=local-development-only-secret-key-change-me
# 使用 SSH 仓库时填写受信任 known_hosts 文件的绝对路径；不要关闭主机密钥校验。
TESTWEAVE_GIT_KNOWN_HOSTS_FILE=

# 本机 Android MCP 只读监看。运行时安装在仓库外，固定 android-mcp-server@1.3.0 与 Node 24 LTS。
# 未配置完整绝对路径时保持 disabled，不影响其他 TestWeave API。
TESTWEAVE_ANDROID_MCP__ENABLED=false
TESTWEAVE_ANDROID_MCP__NODE_PATH=
TESTWEAVE_ANDROID_MCP__ENTRYPOINT=
TESTWEAVE_ANDROID_MCP__CWD=
TESTWEAVE_ANDROID_MCP__TIMEOUT_SECONDS=15
# 真实设备基线通过后再显式开启；默认约 2 FPS，截图慢于间隔时不并发堆叠。
TESTWEAVE_ANDROID_MCP__STREAM_ENABLED=false
TESTWEAVE_ANDROID_MCP__STREAM_INTERVAL_MS=500
TESTWEAVE_ANDROID_MCP__STREAM_IDLE_GRACE_SECONDS=3
TESTWEAVE_ANDROID_MCP__STREAM_DEVICE_RECHECK_SECONDS=5
TESTWEAVE_ANDROID_MCP__STREAM_MAX_BACKOFF_SECONDS=5

# 留空时客户端通过 Vite 同源代理访问服务端。
VITE_API_BASE_URL=
