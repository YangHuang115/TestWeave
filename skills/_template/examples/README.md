# 示例约定

复制模板后提供脱敏、可验证的案例：

- `success-*.json`：正常结构化输入和期望输出；
- `boundary-*.json`：空值、上限、冲突、歧义等边界；
- `failure-*.json`：输入无效、权限不足、Tool/模型失败或校验失败；
- `injection-*.json`：文档或知识库试图改变系统指令的案例。

每个案例注明 Skill/Schema 版本、预期状态和证据要求。示例不是评估通过证明，也不得包含真实秘密或未经授权数据。
